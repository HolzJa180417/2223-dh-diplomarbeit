//checks if password is valid
class CheckPassword {
  final String passwd;
  const CheckPassword({required this.passwd});

  String getPassword() {
    return passwd;
  }

  bool checkIfPasswordIsValid() {
    //implement method to check if password is valid
    return true;
  }
}
